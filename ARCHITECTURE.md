# FitFlow Architecture & Flow Diagrams

## 🏗️ System Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                       FitFlow Frontend (Next.js)                │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │                   Browser / Client                       │   │
│  │  ┌──────────────────────────────────────────────────┐   │   │
│  │  │          Next.js Application (TypeScript)        │   │   │
│  │  │                                                   │   │   │
│  │  │  ┌─────────────────────────────────────────┐     │   │   │
│  │  │  │     Pages (8 Total)                     │     │   │   │
│  │  │  │  ├─ Home (/)                            │     │   │   │
│  │  │  │  ├─ Login (/login)                      │     │   │   │
│  │  │  │  ├─ Register (/register)                │     │   │   │
│  │  │  │  ├─ Dashboard (/dashboard)              │     │   │   │
│  │  │  │  ├─ Workouts (/workouts)                │     │   │   │
│  │  │  │  ├─ Nutrition (/nutrition)              │     │   │   │
│  │  │  │  ├─ Coach (/coach)                      │     │   │   │
│  │  │  │  └─ Progress (/progress)                │     │   │   │
│  │  │  └─────────────────────────────────────────┘     │   │   │
│  │  │                                                   │   │   │
│  │  │  ┌─────────────────────────────────────────┐     │   │   │
│  │  │  │     Components                          │     │   │   │
│  │  │  │  ├─ Navigation                          │     │   │   │
│  │  │  │  └─ DashboardCard                       │     │   │   │
│  │  │  └─────────────────────────────────────────┘     │   │   │
│  │  │                                                   │   │   │
│  │  │  ┌─────────────────────────────────────────┐     │   │   │
│  │  │  │     State Management (Zustand)          │     │   │   │
│  │  │  │  ├─ useAuthStore                        │     │   │   │
│  │  │  │  ├─ useWorkoutStore                     │     │   │   │
│  │  │  │  ├─ useNutritionStore                   │     │   │   │
│  │  │  │  └─ useProgressStore                    │     │   │   │
│  │  │  └─────────────────────────────────────────┘     │   │   │
│  │  │                                                   │   │   │
│  │  │  ┌─────────────────────────────────────────┐     │   │   │
│  │  │  │     Utilities                           │     │   │   │
│  │  │  │  ├─ apiClient (Axios)                   │     │   │   │
│  │  │  │  ├─ store (Zustand stores)              │     │   │   │
│  │  │  │  └─ auth (Auth helpers)                 │     │   │   │
│  │  │  └─────────────────────────────────────────┘     │   │   │
│  │  │                                                   │   │   │
│  │  │  ┌─────────────────────────────────────────┐     │   │   │
│  │  │  │     Styling                             │     │   │   │
│  │  │  │  ├─ Tailwind CSS                        │     │   │   │
│  │  │  │  └─ Global Styles (globals.css)         │     │   │   │
│  │  │  └─────────────────────────────────────────┘     │   │   │
│  │  └──────────────────────────────────────────────────┘   │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                  │
│                    🔄 HTTP Requests (Axios)                     │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
                              ↓↓↓
┌─────────────────────────────────────────────────────────────────┐
│               FitFlow Backend (FastAPI Python)                  │
│                                                                  │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │          API Endpoints (http://localhost:8001)           │   │
│  │                                                          │   │
│  │  Auth:                                                   │   │
│  │    ├─ POST /auth/register                               │   │
│  │    └─ POST /auth/login                                  │   │
│  │                                                          │   │
│  │  Dashboard:                                              │   │
│  │    └─ GET /user/dashboard                               │   │
│  │                                                          │   │
│  │  Workouts:                                               │   │
│  │    ├─ GET /workouts                                     │   │
│  │    ├─ POST /workouts                                    │   │
│  │    └─ POST /workouts/{id}/start                         │   │
│  │                                                          │   │
│  │  Nutrition:                                              │   │
│  │    ├─ GET /nutrition                                    │   │
│  │    ├─ POST /nutrition/log-meal                          │   │
│  │    └─ DELETE /nutrition/meals/{id}                      │   │
│  │                                                          │   │
│  │  AI Coach:                                               │   │
│  │    ├─ GET /coach/chat-history                           │   │
│  │    └─ POST /coach/chat                                  │   │
│  │                                                          │   │
│  │  Progress:                                               │   │
│  │    └─ GET /progress/analytics                           │   │
│  │                                                          │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                  │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │          Database & Services                             │   │
│  │  ├─ User Database                                        │   │
│  │  ├─ Workout Management                                  │   │
│  │  ├─ Nutrition Tracking                                  │   │
│  │  ├─ AI Coach Service                                    │   │
│  │  └─ Analytics Engine                                    │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🔄 User Authentication Flow

```
┌─────────────────┐
│   User Visits   │
│   fitflow.app   │
└────────┬────────┘
         │
         ↓
    ┌─────────────┐
    │ Home Page   │
    │   (Public)  │
    └────┬────────┘
         │
         ├─ [Sign In] ──→ Login Page
         │
         └─ [Register] ──→ Register Page
                           │
                           ↓
                    ┌──────────────┐
                    │ Fill Form    │
                    │ Name, Email, │
                    │ Password,    │
                    │ Age, Level   │
                    └──────┬───────┘
                           │
                           ↓
                    ┌──────────────────┐
                    │ POST /auth/       │
                    │ register         │
                    └──────┬───────────┘
                           │
                    ┌──────────────────┐
                    │ Success?         │
                    └──────┬───────┬───┘
                           │       │
                        Yes│       │No
                           ↓       ↓
                    ┌──────┐  ┌─────────┐
                    │Token │  │ Error   │
                    └──┬───┘  │Message  │
                       │      └─────────┘
                       ↓
            ┌──────────────────────┐
            │ Store Token in       │
            │ localStorage         │
            └──────────┬───────────┘
                       │
                       ↓
            ┌──────────────────────┐
            │ Redirect to          │
            │ Dashboard            │
            └──────────┬───────────┘
                       │
                       ↓
            ┌──────────────────────┐
            │ Dashboard Page       │
            │ (Protected Route)    │
            │ Uses JWT Token       │
            └──────────────────────┘
```

---

## 📊 Component Hierarchy

```
App
├── Layout
│   ├── Navigation
│   │   ├── Logo
│   │   ├── Nav Links (auth)
│   │   └── User Menu
│   │
│   └── Content
│       ├── Home Page
│       │   ├── Hero Section
│       │   ├── Features Grid
│       │   └── CTA Buttons
│       │
│       ├── Login Page
│       │   └── Login Form
│       │
│       ├── Register Page
│       │   └── Registration Form
│       │
│       ├── Dashboard (Protected)
│       │   ├── Header
│       │   ├── Stats Cards
│       │   │   ├── DashboardCard (Workouts)
│       │   │   ├── DashboardCard (Calories)
│       │   │   ├── DashboardCard (Progress)
│       │   │   └── DashboardCard (Streak)
│       │   ├── Recent Workouts
│       │   └── Quick Actions
│       │
│       ├── Workouts Page (Protected)
│       │   ├── Header
│       │   ├── Create Form (optional)
│       │   └── Workouts Grid
│       │
│       ├── Nutrition Page (Protected)
│       │   ├── Macro Stats
│       │   ├── Meal Form (optional)
│       │   └── Meals List
│       │
│       ├── Coach Page (Protected)
│       │   ├── Header
│       │   ├── Chat Messages
│       │   ├── Typing Indicator
│       │   └── Message Input
│       │
│       └── Progress Page (Protected)
│           ├── Stats Cards
│           ├── Weight Chart
│           ├── Workout Chart
│           ├── Calories Chart
│           ├── Types Chart
│           └── Achievements
```

---

## 🔐 Data Flow & State Management

```
┌─────────────────────────────────────────┐
│         User Action                     │
│  (Click button, submit form, etc)       │
└────────────────┬────────────────────────┘
                 │
                 ↓
      ┌──────────────────────┐
      │  Component Handler   │
      │  (onClick, onChange) │
      └──────────┬───────────┘
                 │
                 ├─ Validate Input
                 │
                 ├─ Call API (apiClient)
                 │
                 ↓
      ┌──────────────────────────┐
      │  Axios Interceptor       │
      │  - Add token to header   │
      │  - Handle errors (401)   │
      └──────────┬───────────────┘
                 │
                 ↓
      ┌──────────────────────────┐
      │  FastAPI Backend         │
      │  (http://localhost:8001) │
      └──────────┬───────────────┘
                 │
                 ↓
      ┌──────────────────────────┐
      │  API Response            │
      │  (Success or Error)      │
      └──────────┬───────────────┘
                 │
                 ↓
      ┌──────────────────────────┐
      │  Zustand Store Update    │
      │  (setUser, setWorkouts)  │
      └──────────┬───────────────┘
                 │
                 ↓
      ┌──────────────────────────┐
      │  Component Re-render     │
      │  (with new state)        │
      └──────────┬───────────────┘
                 │
                 ↓
      ┌──────────────────────────┐
      │  Updated UI              │
      │  (shown to user)         │
      └──────────────────────────┘
```

---

## 📱 Responsive Design Breakpoints

```
Mobile First Approach:

Mobile (< 640px)
└── Single column
    └── Full-width cards
        └── Hamburger menu

Small (640px - 768px)
└── Single column
    └── Optimized spacing
        └── Hamburger menu

Medium (768px - 1024px)
└── 2-3 columns
    └── Tablet layout
        └── Horizontal menu

Large (1024px - 1280px)
└── Multi-column grid
    └── Desktop layout
        └── Full navigation

XL (1280px+)
└── Full layout
    └── All features visible
        └── Optimized spacing
```

---

## 🔗 Page Navigation Flow

```
                    ┌─── (Public)
                    │
                    ├── Home (/)
                    │   ├─[Sign In]────→ Login
                    │   └─[Register]──→ Register
                    │
                    │
    Start ──────────┤
                    │
                    ├── Login (/login) (Public)
                    │   ├─ Success ──→ Dashboard
                    │   └─ Register link → Register
                    │
                    │
                    ├── Register (/register) (Public)
                    │   ├─ Success ──→ Dashboard
                    │   └─ Sign in link → Login
                    │
                    │
                    └── Protected Routes (Need Auth)
                        │
                        ├── Dashboard (/dashboard)
                        │   ├─ [Start Workout] → Workouts
                        │   ├─ [Log Meal] → Nutrition
                        │   └─ [Chat] → Coach
                        │
                        ├── Workouts (/workouts)
                        │   ├─ Create workout
                        │   └─ Start workout
                        │
                        ├── Nutrition (/nutrition)
                        │   └─ Log meal
                        │
                        ├── Coach (/coach)
                        │   └─ Chat with AI
                        │
                        ├── Progress (/progress)
                        │   └─ View analytics
                        │
                        └── [Logout]
                            └── Home (/)
```

---

## 📡 API Call Sequence (Example: Dashboard)

```
User Visits /dashboard
       │
       ↓
Component Mounts
       │
       ├─ Check: isAuthenticated?
       │  ├─ No → Redirect to /login
       │  └─ Yes → Continue
       │
       ↓
useEffect Hook Runs
       │
       ├─ Set loading = true
       │
       ↓
API Call: GET /user/dashboard
       │
       ├─ Axios Interceptor
       │ ├─ Get token from localStorage
       │ └─ Add to Authorization header
       │
       ↓
Backend Processes Request
       │
       ├─ Validate token (JWT)
       ├─ Fetch user data
       ├─ Calculate stats
       ├─ Get recent workouts
       └─ Return JSON response
       │
       ↓
Response Received
       │
       ├─ Success (200)?
       │  ├─ Yes → Update state
       │  └─ No → Show error
       │
       ↓
Update Zustand Store
       │ (useAuthStore, etc)
       │
       ↓
Set loading = false
       │
       ↓
Component Re-renders
with fresh data
       │
       ↓
User Sees Dashboard
       │
       └─ Can interact with UI
```

---

## 🎯 Form Submission Flow

```
User Sees Form
       │
       ↓
User Fills Fields
       │
       ↓
User Clicks Submit
       │
       ↓
Form Validation
├─ Email format?
├─ Required fields?
└─ Password length?
       │
       ├─ Invalid → Show errors
       │
       └─ Valid → Continue
              │
              ↓
       Set loading = true
              │
              ↓
       API Request
       (POST to backend)
              │
              ↓
       Backend Processes
       (Hash password, validate, save)
              │
              ↓
       Response
       ├─ Success (201/200)
       │  ├─ Get token & user data
       │  ├─ Store token
       │  ├─ Update Zustand
       │  ├─ Clear form
       │  └─ Redirect
       │
       └─ Error (4xx/5xx)
          └─ Show error message
                 │
                 ↓
              Set loading = false
                 │
                 ↓
            User Can Retry
```

---

## 🎨 Styling Architecture

```
Tailwind CSS Configuration
       │
       ├─ Theme (colors, sizes)
       │  ├─ Primary: Indigo
       │  ├─ Secondary: Pink
       │  └─ Accent: Amber
       │
       ├─ Components
       │  ├─ .btn-primary
       │  ├─ .btn-secondary
       │  ├─ .card
       │  ├─ .input-field
       │  └─ .gradient-bg
       │
       ├─ Utilities
       │  ├─ Spacing
       │  ├─ Sizing
       │  ├─ Colors
       │  └─ Effects
       │
       └─ Global Styles (globals.css)
          ├─ @tailwind directives
          └─ Custom CSS variables

              ↓
              
        HTML Elements
        (Get classes applied)
        
              ↓
              
        Styled Components
        (Rendered to user)
```

---

**For more details, refer to the complete documentation files.**
